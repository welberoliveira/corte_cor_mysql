@page
@model PainelModel

<style>
    .tdStatusPendente {
        color: red;
    }

    .tdStatusAprovado {
        color: green;
    }
</style>

<style>
    .charts-container {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
    }

    .chart {
        flex: 1 1 300px;
        max-width: 400px;
        height: 250px;
        margin: 10px;
    }
</style>

<div class="container mt-1" style="height: 400px;">
    <h4 class="text-center mb-4">Painel Principal</h4>
    <div class="form-group col-md-4">
        <label for="estadoCivil">Selecione a Empresa:</label>
        <select class="form-control" id="IdSalao" name="IdSalao">
            <option value="11">Empresa Paula Beauty</option>
            <option value="1">Flor de Mulher Beauty</option>
            <option value="2">Encanto Rosa Beauty</option>
            <option value="3">Divina Bela Studio</option>
            <option value="4">Bella Morena EspaÃ§o de Beleza</option>
            <option value="5">Luna Rosa Beauty</option>
            <option value="6">Doce Brilho Beauty</option>
            <option value="7">EssÃªncia Feminina</option>
            <option value="8">Rainha do Espelho</option>
            <option value="9">Beleza Tropical</option>
            <option value="10">Linda & Leve Studio</option>
        </select>
    </div>

    <button class="btn btn-info">Acessar como esta Empresa</button>


    <!-- Carrega o Google Charts -->
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script>
        google.charts.load('current', { 'packages': ['corechart'] });
        google.charts.setOnLoadCallback(drawAllCharts);

        function drawAllCharts() {
            drawChart('chartDocumentacao', 'DocumentaÃ§Ã£o do ImÃ³vel', getDataArray(@Html.Raw(Json.Serialize(Model.DocumentacaoChartData)), 'DocumentaÃ§Ã£o'));
            drawChart('chartCondicaoOcupacaoLote', 'CondiÃ§Ã£o de OcupaÃ§Ã£o do Lote', getDataArray(@Html.Raw(Json.Serialize(Model.CondicaoOcupacaoLoteChartData)), 'CondiÃ§Ã£o de OcupaÃ§Ã£o do Lote'));
            drawChart('chartPossuiIPTU', 'Possui IPTU', getDataArray(@Html.Raw(Json.Serialize(Model.PossuiIPTUChartData)), 'Possui IPTU'));
        }

        // Converte o objeto JSON em um array adequado para o Google Charts
        function getDataArray(chartData, labelName) {
            var arr = [];
            arr.push([labelName, 'Quantidade']);
            for (var key in chartData) {
                if (chartData.hasOwnProperty(key)) {
                    arr.push([key, chartData[key]]);
                }
            }
            return arr;
        }

        // Desenha um grÃ¡fico de pizza 3D
        function drawChart(elementId, title, dataArray) {
            var data = google.visualization.arrayToDataTable(dataArray);
            var options = {
                title: title,
                is3D: true,
                width: '100%',
                height: '100%'
            };
            var chart = new google.visualization.PieChart(document.getElementById(elementId));
            chart.draw(data, options);
        }
    </script>

</div>


