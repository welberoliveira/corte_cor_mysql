@page
@model ErrorModel
@{
    ViewData["Title"] = "Erro";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <div class="alert alert-danger" role="alert">
                <h4 class="alert-heading">
                    <i class="bi bi-exclamation-triangle-fill"></i> Ops! Algo deu errado.
                </h4>
                <hr>
                <p class="mb-0">
                    Ocorreu um erro inesperado. Nossa equipe de suporte técnico já foi notificada 
                    e está trabalhando na solução.
                </p>
            </div>

            @if (Model.ShowRequestId)
            {
                <p class="text-muted small mt-3">
                    <strong>Código de Referência:</strong> <code>@Model.RequestId</code>
                    <br>
                    <em>Informe este código ao suporte caso entre em contato.</em>
                </p>
            }

            <a href="@Url.Content("~/Agendamentos2")" class="btn btn-primary mt-3">
                <i class="bi bi-house"></i> Voltar ao Início
            </a>
        </div>
    </div>
</div>
