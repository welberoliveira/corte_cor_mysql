@page
@model CorteCor.Pages.CategoriaProdutoCadastroModel
@{
    ViewData["Title"] = "Cadastro de Categoria de Produtos";
}

<div class="container mt-4">
    <h4>@Model.ButtonText Categoria de Produto</h4>

    @if (!string.IsNullOrEmpty(Model.Mensagem))
    {
        <div class="alert alert-success mt-3">@Model.Mensagem</div>
    }

    <form method="post" class="mt-4">
        <input type="hidden" name="id" value="@Model.Categoria?.IdCategoria">
        <input type="hidden" name="action" value="salvar">

        <div class="row">
            <div class="col-md-9 form-group">
                <label for="nome" class="form-label">Nome da Categoria</label>
                <input type="text" id="nome" name="nome" class="form-control" required value="@Model.Categoria?.Nome">
            </div>
            <div class="col-md-3 form-group d-flex align-items-end mb-3">
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" id="ativo" name="ativo" @(Model.Categoria == null || Model.Categoria.Ativo ? "checked" : "")>
                    <label class="form-check-label" for="ativo">Ativo</label>
                </div>
            </div>
        </div>

        <div class="mt-4">
            <button type="submit" class="btn btn-primary">@Model.ButtonText</button>
            <a asp-page="/CategoriaProdutoLista" class="btn btn-secondary ms-2">Voltar</a>
        </div>
    </form>
</div>
